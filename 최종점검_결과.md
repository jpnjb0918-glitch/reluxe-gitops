# 최종 점검 결과

> 2026-09-04 · 팀원 지적 2건 반영 후 전체 재점검

---

## 점검 방법

형식뿐 아니라 **문서가 주장하는 내용이 실제 파일과 맞는지**까지 확인했습니다.

| 단계 | 확인 |
|---|---|
| 문서 ↔ 실제 파일 | 각 문제의 해결책이 실제로 코드에 있는가 |
| 인용 명령 | 문서에 적은 `kubectl patch` JSON 이 유효한가 |
| 건수 정합 | 분류 표 합계 = 실제 문제 수 |
| 스크립트 순서 | `20-trigger.sh` 가 부르는 9개가 전부 존재하는가 |
| 포트 충돌 | 여러 스크립트가 같은 NodePort 를 쓰지 않는가 |
| 인코딩 | BOM · 깨진 문자 |

---

## 🔴 이번에 추가로 발견한 것

### ① 문서마다 문제 건수가 달랐습니다

| 문서 | 표기 | 실제 |
|---|:--:|:--:|
| `문제해결_기록.md` 분류 표 | 18 | **19** |
| `팀공유_구축테스트정리.md` | 16 | **19** |
| `구축테스트_과정기록.md` | **11** | **19** |
| `적용방법.md` | 16 | **19** |

문제를 추가할 때 **본문만 고치고 요약·표를 안 고친 것**이 쌓였습니다.
특히 `구축테스트_과정기록.md` 는 11건에서 멈춰 있었습니다.

**전부 19로 맞췄고, 분류도 다시 셌습니다.**

| 분류 | 건수 |
|---|:--:|
| 환경 · 도구 문제 | 7 |
| **우리 코드 · 설정의 실수** | **8** |
| 이해가 부족했던 것 | 4 |

### ② 서로 다른 사본에서 작업해 한쪽이 누락됐습니다

서브차트 수정과 CRLF 수정을 **각각 다른 clone 에서** 하는 바람에,
합치기 전에는 한쪽에 다른 쪽 변경이 빠져 있었습니다. 병합 후 확인했습니다.

### ③ `.gitignore` 에 `vagrant/.vagrant/` 가 없었습니다

그 안에 **VM 접속용 SSH 개인키**가 들어 있습니다.
지금은 추적에서 제외됐지만, 규칙이 없으면 **다시 커밋될 수** 있습니다.

```
# Vagrant 상태 (VM 접속용 SSH 키 포함 — 절대 커밋 금지)
vagrant/.vagrant/
vagrant/build.log
```

---

## 검증 통과 항목

### 문서 주장 ↔ 실제 파일

| 문제 | 주장 | 실제 |
|:--:|---|:--:|
| 3-② | Vagrantfile 에 CRLF provisioner (file → fix → common) | ✅ |
| 8 | migrate-job 이 ConfigMap 을 `optional` 로 | ✅ |
| 9 | deployment 에 `matchLabelKeys` | ✅ |
| 12 | postgres-cluster 에 `secret` 미명시 | ✅ |
| 17 | `10-k3s-server.sh` 에 전체 경로 | ✅ |
| 18 | toleration `effect` 2종 | ✅ |
| 19 | `nodeExporter` 에서 `tolerations` 제거 | ✅ |

### 구조

| | |
|---|---|
| 문제 1~19 연속 · 분류 표 합계 19 | ✅ |
| 스크립트 13개 · 실행 순서 9단계 전부 존재 | ✅ |
| 포트 중복 없음 (30081 · 30300 · 30443 · 30500 · 30808) | ✅ |
| 차트 `config` 36개 · 환경별 분기 | ✅ |
| 셸 문법 · YAML 파싱 · 인코딩 | ✅ |

### 정리 확인

| | |
|---|---|
| `vagrant/build.log` · 깨진 문서 2개 · `.vagrant/` | ✅ 없음 |
| `.gitignore` BOM 없음 · 규칙 26개 | ✅ |

---

---

## 🔴 4차 점검 — 실행 시 위험 지점

이번엔 각도를 바꿔 **"실제로 돌렸을 때 실패할 곳"** 을 봤습니다.

### 스크립트 간 값 일치 — 전부 통과

한 곳만 고치면 깨지는 것들입니다.

| 항목 | |
|---|:--:|
| k3s 토큰 (서버 ↔ 에이전트) | ✅ |
| 서버 주소 (`192.168.56.10`) ↔ Vagrantfile node0 | ✅ |
| `--flannel-iface=enp0s8` 양쪽 | ✅ |
| 레지스트리 주소 ↔ 차트 `image.repository` | ✅ |
| 빌드 태그(`dev`) ↔ 차트 `image.tag` | ✅ |
| 네임스페이스 · 라벨 | ✅ |
| NodePort 중복 없음 | ✅ |

### ⚠️ 발견 — Jenkins 플러그인 버전이 `:latest`

```yaml
installPlugins:
  - kubernetes:latest
  - workflow-aggregator:latest
  - git:latest
  - configuration-as-code:latest
```

**우리가 검증한 시점(2026-09-04)에는 정상 동작했습니다.**
다만 시간이 지나면 조합이 달라지고, **의존성이 충돌하면 Jenkins 가 아예 뜨지 않습니다.**

jenkinsci/helm-charts 에 반복 보고된 문제입니다 (#219 · #704 · #911).

**판단 — 그대로 두되 대응법을 적었습니다.**

특정 버전을 박아도 그 버전이 사라지거나 다른 플러그인이 더 높은 버전을 요구하면
똑같이 깨집니다. 운영이라면 플러그인을 미리 구운 커스텀 이미지를 쓰는 것이 맞지만,
학습 · 시연 환경에서는 과합니다.

**대신 이렇게 준비했습니다.**

| | |
|---|---|
| `90-jenkins.sh` | 위험과 대응법을 주석으로 |
| `Vagrant_실행가이드.md` | 문제 해결 표에 추가 · 복구 절차 |

```bash
# Jenkins 가 Init 에서 멈추면
kubectl logs -n infra jenkins-0 -c init
# "depends on X, but there is an older version" → 의존성 충돌
# → installLatestPlugins: false 로 차트 기본 버전 사용
```

> Jenkins 는 CI 용이라 나머지와 독립입니다.
> 여기서 막혀도 클러스터 · 앱 · DB · Argo CD · 모니터링은 이미 동작합니다.

### 그 외 확인

| 항목 | 판정 |
|---|---|
| `set -e` 상태의 `kubectl` 12건 | 대부분 `kubectl wait` 또는 조회 — 실패 시 멈추는 게 맞음 |
| 외부 네트워크 의존 9곳 | get.k3s.io · helm repo · github — 불가피 |
| `kubectl exec` (Jenkins 비밀번호) | `\|\| true` 있어 안전 |

---

## 5차 점검 — 아직 안 본 파일과 템플릿 렌더링

### 대상

지금까지 한 번도 열어보지 않은 파일 13개를 봤습니다.

```
Chart.yaml · README.md ×2 · _helpers.tpl
hpa · ingress · pdb · service · aggregate-cronjob
pgdump-cronjob · crawler-cronjob
infra/registries.yaml · infra/minio.yaml
```

### 결과 — 전부 정상

| 항목 | |
|---|:--:|
| 템플릿이 참조하는 `.Values` **81개** 전부 존재 | ✅ |
| `_helpers.tpl` define ↔ include 일치 (미정의 · 미사용 0) | ✅ |
| 조건부 리소스 `enabled` 플래그 환경별 값 | ✅ |
| `crawler-cronjob` 의 `/dev/shm` · `medium: Memory` · `sizeLimit` | ✅ |
| 크롤러 진입점 `app.crawler --once` ↔ **실제 저장소에 `__main__.py` 존재** | ✅ |
| `aggregate` · `pgdump` 비활성 판단 ↔ **실제로 모듈 없음** | ✅ |
| `30-registry.sh` 가 만드는 설정 ↔ `infra/registries.yaml` **완전 동일** | ✅ |
| `vagrant/README.md` 포트 · 스크립트 참조 | ✅ |
| `charts/reverdi/README.md` DB 서비스 이름 · `reluxe` 잔재 0 | ✅ |
| `infra/minio.yaml` toleration · Secret 생성 안내 주석 | ✅ |

### 오탐 3건

검사 자체가 틀렸던 것들입니다.

| 검사 | 왜 틀렸나 |
|---|---|
| "크롤러 진입점 없음" | 템플릿만 봤는데 실제로는 `values.crawler.command` 에 있음 |
| "`registries.yaml` 불일치" | 정규식으로 셸 변수를 추출하려 함 → 셸에서 직접 실행해 확인하니 동일 |
| "`minio-credentials` 만드는 곳 없음" | `minio.yaml` 주석에 생성 명령이 이미 있음 |

**검사 로직의 오류였고, 파일은 정상입니다.**

---

## 점검 5회 정리

| 회차 | 각도 | 발견 |
|:--:|---|---|
| 1 | 팀원 지적 | 서브차트 키 |
| 2 | 형식 정합 | 문서 건수 4곳 불일치 |
| 3 | 내용 대조 | `.gitignore` `.vagrant/` 누락 |
| 4 | 실행 위험 | Jenkins 플러그인 `:latest` |
| 5 | **미확인 파일 · 템플릿** | **없음 (오탐 3건)** |

**5차에서 새 문제가 나오지 않았습니다.**
1~4차에서 나온 것은 전부 다른 각도였는데, 5차는 남은 영역을 훑었고 깨끗했습니다.

> 더 파면 나오는 건 "지금은 맞지만 시간이 지나면 깨질 수 있는 것"(외부 의존성) 뿐입니다.
> 그건 문서로 대응하는 게 맞습니다 — 4차에서 그렇게 했습니다.

---

## push 할 파일 10개

| 파일 | 변경 |
|---|---|
| `helm-values/kube-prometheus-stack.yaml` | 서브차트 키 |
| `vagrant/scripts/80-monitoring.sh` | 〃 (인라인) |
| `vagrant/Vagrantfile` | CRLF provisioner |
| `.gitignore` | `vagrant/.vagrant/` 추가 |
| `문제해결_기록.md` | 19건 · 분류 표 |
| `팀공유_구축테스트정리.md` | 16 → 19 |
| `구축테스트_과정기록.md` | 11 → 19 |
| `적용방법.md` | 16 → 19 |
| `Vagrant_실행가이드.md` | **신규** |
| `AWS구성도_슬라이드.drawio` | **신규** (16:9 · 글자 최소화) |

```powershell
cd D:\project\Reluxe
# 압축 해제 (7-Zip · 반디집)

git add -A
git status
git commit -m "fix: 서브차트 키·CRLF 자동제거, 문서 건수 정합, 실행 가이드 추가"
git push
```

### 확인

```powershell
git check-ignore -v --no-index vagrant/.vagrant/test.txt
```

`.gitignore:NN:vagrant/.vagrant/` 가 나오면 규칙이 걸린 겁니다.

---

## 남은 것 (선택)

| 항목 | 비고 |
|---|---|
| Jenkins `update gitops` | `when { branch 'main' }` 로 건너뜀 — Multibranch 로 바꾸면 해결 |
| MinIO | 배포 전 |
| `pgdump` · `aggregate` CronJob | 진입점 미구현 |
| AWS `<S3_BUCKET_NAME>` | 이관 전 필수 |
| ALB `xff_header_processing.mode = replace` | 이관 시 필수 (보안업데이트 문서 참조) |

**로컬 검증에는 지장이 없습니다.**
