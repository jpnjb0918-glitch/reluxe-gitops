# Vagrant 클러스터 구축 — 작업 기록 및 수정 사항

> 실제 구축하며 겪은 문제와 그에 따른 파일 수정 내역입니다.
> **팀에 공유해주세요.**

---

## 결과

VM 6대에 k3s 클러스터를 세우고 앱 배포까지 완료했습니다.

```
node0  컨트롤 플레인 (taint 격리)
node1~3  웹 파드 3개 + DB 파드 3개
node4  배치 (이미지 빌드)
node5  레지스트리
```

| 검증 항목 | 결과 |
|---|---|
| 웹 파드 분산 | node1·2·3 에 **하나씩** (`topologySpreadConstraints` 동작) |
| DB 고가용성 | primary 1 + replica 2, **다른 노드에 분산** |
| 마이그레이션 순서 | Helm 훅으로 앱보다 **먼저** 실행 |
| `/ready` 응답 | `database.connected: true` · `migration.up_to_date: true` |
| 웹 접속 | `http://192.168.56.11:30080` **200** |

---

## 🔴 수정한 파일

### 1. `charts/reluxe/templates/migrate-job.yaml`

**증상**
```
Error: configmap "reluxe-config" not found
→ CreateContainerConfigError
```

**원인**
`migrate-job` 은 `pre-install` 훅이라 일반 리소스(ConfigMap)보다 **먼저** 실행된다.
첫 설치 때는 ConfigMap 이 아직 없어서 파드가 뜨지 못한다.

**수정**
마이그레이션에 실제로 필요한 값은 `DATABASE_URL` 하나뿐이고 그건 Secret 에 있다.
`LOG_LEVEL` 같은 값은 `config.py` 에 전부 기본값이 있어 없어도 동작한다.

```yaml
envFrom:
  - secretRef:
      name: {{ include "reluxe.secretName" . }}
  - configMapRef:
      name: {{ include "reluxe.configMapName" . }}
      optional: true        # ← 없으면 건너뛴다
```

> ⚠️ **ConfigMap 에 훅을 거는 방법은 쓰지 않았습니다.**
> 그렇게 하면 `deployment.yaml` 의 `checksum/config` 가 무의미해지고,
> ConfigMap 이 릴리스에서 빠져 `helm rollback` 대상이 되지 않습니다.

### 2. `charts/reluxe/templates/secret.yaml`

`secret.create: true` 로 차트가 Secret 을 만들 때도 같은 순서 문제가 생깁니다.
`hook-weight: -10` 을 줘서 migrate(`-5`)보다 먼저 만들어지게 했습니다.

기본값 `create: false` 에서는 렌더링되지 않으므로 영향 없습니다.

### 3. `vagrant/scripts/k3s-server.sh` · `k3s-agent.sh`

**증상**
```
failed calling webhook "mcluster.cnpg.io": context deadline exceeded
curl -k https://<다른노드 파드IP>:9443/readyz  →  exit=28 (타임아웃)
```

**원인 — 오늘 가장 찾기 어려웠던 문제**

`ip -d link show flannel.1` 결과가 이랬습니다.

```
vxlan id 1 local 10.0.2.15 dev enp0s3
```

Flannel 이 **Vagrant NAT 인터페이스**(`enp0s3`, `10.0.2.15`)를 잡았습니다.
이 IP 는 **6대 전부 동일**해서 VXLAN 터널이 맺히지 않습니다.

라우팅 테이블은 정상이고 방화벽도 정상인데 패킷만 안 가는,
**"파드는 뜨는데 노드 간 통신만 안 되는"** 상태가 됩니다.

**`--node-ip` 만으로는 부족합니다.**
그건 "쿠버네티스가 보는 노드 주소"만 바꾸고, Flannel 이 쓰는 인터페이스는 별개입니다.

**수정**
```bash
--flannel-iface=enp0s8    # 사설망 인터페이스
--node-ip=192.168.56.1x
```

**확인 방법**
```bash
ip -d link show flannel.1 | grep vxlan
# local 192.168.56.x 여야 정상
```

### 4. `vagrant/scripts/common.sh`

**증상**
```
tee: /etc/rancher/k3s/registries.yaml: No such file or directory
```

k3s **서버**는 설치 때 `/etc/rancher/k3s/` 를 만들지만 **에이전트는 만들지 않습니다.**
`mkdir -p /etc/rancher/k3s` 를 추가했습니다.

### 5. `vagrant/Vagrantfile`

| 항목 | 수정 |
|---|---|
| 박스 | `rockylinux/9` → **`bento/rockylinux-9`** |
| 부팅 타임아웃 | 기본 300초 → **900초** |
| `cpuexecutioncap` | **제거** |

**박스 404** — Rocky 마이너 버전이 Vault 로 옮겨졌는데 Vagrant 레지스트리는 옛 경로를 가리킵니다. 2024년부터 반복 신고된 문제입니다.

**타임아웃** — node5 는 RAM 16GB + 디스크 100GB 리사이즈가 겹쳐 첫 부팅이 오래 걸립니다.
`cpuexecutioncap` 은 제가 호스트를 배려하려고 넣었는데, `dnf install` 을 느리게 만들어
오히려 타임아웃의 원인이 됐습니다.

### 6. `vagrant/README.md`

실제 겪은 문제 5개를 "자주 막히는 곳" 표에 추가했습니다.

---

## ⚠️ 수정하지 않았지만 알아야 할 것

### `reluxe-db-app` Secret 은 직접 만들어야 합니다

`infra/postgres-cluster.yaml` 주석에는 "없으면 CNPG 가 자동 생성한다"고 되어 있는데,
**`bootstrap.initdb.secret.name` 을 명시하면 "이미 있는 것을 쓰겠다"는 뜻**이 되어
자동 생성되지 않습니다.

```bash
kubectl create secret generic reluxe-db-app -n reluxe \
  --type=kubernetes.io/basic-auth \
  --from-literal=username=reluxe \
  --from-literal=password="$(openssl rand -hex 16)"
```

> `--type=kubernetes.io/basic-auth` 가 중요합니다. CNPG 가 이 형식을 기대합니다.

주석을 고치거나, `secret` 블록을 빼서 자동 생성에 맡기는 것 중 하나를 팀에서 정하면 좋겠습니다.

### `sudo k3s` 가 안 됩니다

RHEL 계열의 `sudo` 는 `secure_path` 에서 `/usr/local/bin` 을 제외합니다.

```bash
sudo /usr/local/bin/k3s kubectl get nodes    # 전체 경로
kubectl get nodes                            # kubeconfig 설정 후 (권장)
```

**EKS 워커 노드(Amazon Linux 2023)도 같습니다.**

### PowerShell → VM 파일 전송

`Get-Content ... | vagrant ssh` 파이프가 자주 멈춥니다.
그리고 CRLF 때문에 `set: pipefail\r: invalid option name` 오류가 납니다.

**권장 순서**
1. VM 안에서 `git clone`
2. `vagrant ssh node0` 으로 들어가 리눅스 셸에서 작업
3. 노드 간 배포는 node0 에서 `ssh`/`scp`

첫 작업 때 SSH 키를 복사해두면 훨씬 편합니다.

```bash
ssh-keygen -t ed25519 -N "" -f ~/.ssh/id_ed25519
ssh-copy-id vagrant@192.168.56.11    # 노드마다 한 번씩
```

---

## 아직 안 한 것

| 항목 | 비고 |
|---|---|
| 크롤러 이미지 | 3.59GB. `/dev/shm` 검증이 목적 |
| Jenkins · Argo CD | README 13단계 |
| DB 페일오버 시연 | `kubectl delete pod reluxe-db-1` → 자동 승격 확인 |
| 앱 읽기/쓰기 분리 | **백엔드 수정 필요.** 지금은 `DATABASE_RO_URL` 을 앱이 안 읽음 |
| `aggregate` · `pgdump` CronJob | 진입점 미구현이라 `enabled: false` |

---

## 🔴 커밋하면 안 되는 것

Secret 값은 **`kubectl create` 로만** 만듭니다. git 에 넣지 마세요.

```
reluxe-db-app   DB 계정
reluxe-secret   앱 환경변수 (DATABASE_URL · SESSION_SECRET · 로그인 계정)
```
