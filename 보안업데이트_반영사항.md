# 백엔드 보안 업데이트 반영 — 담당별 수정 사항

> 커밋 `17445fc` (로그인 실패 잠금 + 실시간 검색 호출 제한)
> 커밋 `9af5e14` (CSV 업로드 URL 덮어쓰기 차단 + 판매자 기준 수정 권한)
> 확인 시각: 2026-09-04

---

## 0. 요약

백엔드가 **환경변수 11개**를 새로 추가했습니다. 그중 **3개는 인프라 설계에 직접 영향**을 줍니다.

| 담당 | 수정 파일 | 건수 |
|:--:|---|:--:|
| **A** | `values.yaml` · `values-vagrant.yaml` · `values-aws.yaml` | **11개 키 추가 + 환경별 분기 3건** |
| **B** | 없음 | — |
| **C** | `postgres-cluster.yaml` (선택) · AWS 이관 시 CA 번들 | **1~2건** |

---

## 1. 새로 생긴 환경변수 11개

### 속도 제한 (8개) — 값만 추가하면 됨

| 키 | 기본값 | 의미 |
|---|---:|---|
| `LOGIN_MAX_FAILURES` | 5 | 같은 IP 가 몇 번 틀리면 잠그나 |
| `LOGIN_LOCKOUT_SECONDS` | 300 | 잠금 유지 시간 |
| `LIVE_SEARCH_RATE_LIMIT` | 10 | 실시간 검색 호출 상한 |
| `LIVE_SEARCH_RATE_WINDOW_SECONDS` | 60 | 그 창(window) |
| `CLICK_RATE_LIMIT` | 60 | 클릭 이벤트 상한 |
| `CLICK_RATE_WINDOW_SECONDS` | 60 | |
| `CLICK_NEW_SESSION_LIMIT` | 30 | 새 세션 발급 상한 |
| `CLICK_NEW_SESSION_WINDOW_SECONDS` | 3600 | |

> 전부 기본값이 있어 **안 넣어도 동작**합니다. 다만 값을 바꾸려면 이미지를 다시 빌드해야 하므로
> ConfigMap 에 올려두는 편이 낫습니다.

### 🔴 인프라 영향이 있는 3개

| 키 | 기본값 | 왜 중요한가 |
|---|---|---|
| **`TRUST_PROXY_HEADERS`** | `IS_PRODUCTION` | ALB 뒤에서 클라이언트 IP 를 어떻게 볼지 |
| **`DATABASE_SSL_MODE`** | `prefer` | RDS 연결이 평문일 수 있음 |
| **`DATABASE_SSL_ROOT_CERT`** | (빈 값) | 인증서 검증을 하려면 CA 번들 마운트 필요 |

---

## 2. 🔴 A 담당 — 반드시 고쳐야 할 것

### ① `TRUST_PROXY_HEADERS` — 안 켜면 서비스가 막힙니다

`app/ratelimit.py` 의 주석이 상황을 정확히 설명합니다.

> ALB 뒤에서는 `request.client.host` 가 전부 ALB 의 IP 라, 그것으로 세면
> **모든 사용자가 한 사람으로 묶여 첫 사용자가 상한을 채우면 전원이 막힌다.**

**로컬(NodePort)** — 프록시가 없으므로 `false`
**AWS(ALB)** — `true` 여야 함

기본값이 `IS_PRODUCTION` 이라 `APP_ENV=production` 이면 자동으로 켜지지만,
**명시하는 편이 안전**합니다. 나중에 `APP_ENV` 를 건드릴 때 같이 바뀌면 곤란합니다.

```yaml
# values-vagrant.yaml
TRUST_PROXY_HEADERS: "false"   # 프록시 없음 — 헤더를 믿으면 누구나 IP 를 위조해 제한을 피한다

# values-aws.yaml
TRUST_PROXY_HEADERS: "true"    # ALB 가 X-Forwarded-For 를 붙인다
```

### ② `DATABASE_SSL_MODE` — 기본값이 위험합니다

기본이 `prefer` 입니다. **서버가 TLS 를 안 받으면 평문으로 붙습니다.**

코드에도 경고가 있습니다.

```python
if IS_PRODUCTION and DATABASE_SSL_MODE in ("disable", "allow", "prefer"):
    logger.warning("DATABASE_SSL_MODE=%s 로 운영 기동 — DB 연결이 평문일 수 있습니다.")
```

**경고만 하고 기동은 됩니다.** 우리가 값을 안 주면 그냥 지나갑니다.

```yaml
# values-vagrant.yaml
DATABASE_SSL_MODE: "prefer"    # CloudNativePG 는 TLS 를 자동 구성하지만 로컬은 검증 불필요

# values-aws.yaml
DATABASE_SSL_MODE: "require"   # 🔴 최소 require. VPC 안이라도 평문은 두지 않는다
```

> **`verify-full` 을 쓰려면** RDS 루트 CA 번들을 파드에 마운트해야 합니다 (아래 C 담당 참조).
> 그 준비가 안 됐으면 `require` 로 시작하는 게 맞습니다.

### ③ `values.yaml` 에 11개 키 추가

`config` 블록이 25개 → **36개**가 됩니다.

```yaml
config:
  # ... 기존 25개 ...

  # -------------------------------------------------------------------------
  # 속도 제한 (2026-09-03 보안 업데이트)
  #
  # ⚠️ 이 제한은 파드 하나가 자기 메모리로 센다 (app/ratelimit.py).
  #    파드가 3개면 상한이 사실상 3배로 느슨해진다.
  #    코드 주석도 그걸 인정하고 있다 — "무제한과 분당 30회는 전혀 다른 상태"
  #    파드를 가로지르는 제한은 AWS WAF rate-based rule 몫이다.
  # -------------------------------------------------------------------------
  LOGIN_MAX_FAILURES: "5"
  LOGIN_LOCKOUT_SECONDS: "300"
  LIVE_SEARCH_RATE_LIMIT: "10"
  LIVE_SEARCH_RATE_WINDOW_SECONDS: "60"
  CLICK_RATE_LIMIT: "60"
  CLICK_RATE_WINDOW_SECONDS: "60"
  CLICK_NEW_SESSION_LIMIT: "30"
  CLICK_NEW_SESSION_WINDOW_SECONDS: "3600"

  # 🔴 ALB 뒤에서 클라이언트 IP 를 어떻게 볼지. 환경별로 반드시 다르다.
  TRUST_PROXY_HEADERS: "false"

  # 🔴 DB 연결 암호화. 기본 prefer 는 서버가 거부하면 평문으로 붙는다.
  DATABASE_SSL_MODE: "prefer"
  DATABASE_SSL_ROOT_CERT: ""
```

---

## 3. B 담당 — 고칠 것 없음

크롤러 · 마이그레이션 · CronJob 은 영향받지 않습니다.

속도 제한은 **HTTP 요청 경로에만** 걸립니다. 배치 워크로드는 대상이 아닙니다.

> 다만 `migrate-job` 이 `envFrom` 으로 ConfigMap 을 통째로 받으므로,
> A 가 키를 추가하면 자동으로 따라갑니다. **별도 작업 불필요**합니다.

---

## 4. C 담당 — 확인 1건, AWS 이관 시 1건

### ① (선택) 로컬 DB 의 TLS

CloudNativePG 는 **기본으로 TLS 인증서를 자동 생성**합니다.
우리 클러스터에도 이미 있습니다.

```
reverdi-db-ca            Opaque
reverdi-db-replication   kubernetes.io/tls
reverdi-db-server        kubernetes.io/tls
```

`DATABASE_SSL_MODE=require` 로 올려도 로컬에서 붙습니다.
**검증까지 하려면** `reverdi-db-ca` 를 파드에 마운트해야 하는데, 로컬에서는 과합니다.

**판단** — 로컬은 `prefer` 로 두고, AWS 에서 `require` 부터 시작하면 됩니다.

### ② 🔴 AWS 이관 시 — RDS 루트 CA 번들

`verify-full` 을 쓰려면 파드에 CA 파일이 있어야 합니다.

```
https://truststore.pki.rds.amazonaws.com/ap-northeast-2/ap-northeast-2-bundle.pem
```

**방법 두 가지**

**A. ConfigMap 에 넣고 마운트** (간단)
```bash
curl -o rds-ca.pem https://truststore.pki.rds.amazonaws.com/ap-northeast-2/ap-northeast-2-bundle.pem
kubectl create configmap rds-ca -n reverdi --from-file=rds-ca.pem
```
```yaml
# deployment.yaml 에 추가 (A 담당과 협의)
volumeMounts:
  - { name: rds-ca, mountPath: /etc/ssl/rds, readOnly: true }
volumes:
  - name: rds-ca
    configMap: { name: rds-ca }
```
```yaml
DATABASE_SSL_ROOT_CERT: "/etc/ssl/rds/rds-ca.pem"
DATABASE_SSL_MODE: "verify-full"
```

**B. 이미지에 굽기** — 인증서가 바뀌면 재빌드가 필요해 권하지 않음

> **우선순위는 낮습니다.** `require` 만으로도 전송 구간은 암호화됩니다.
> `verify-full` 은 "중간자가 다른 서버로 유도하는 것"까지 막는 단계입니다.
> 시연 범위를 넘어서니, **문서에 계획만 적어두고 나중에** 해도 됩니다.

---

## 5. ⚠️ 인프라 설계에 남는 과제 — 발표에서 물어볼 만한 것

### 속도 제한이 파드 메모리에 있습니다

`app/ratelimit.py` 의 첫 줄이 이렇게 시작합니다.

> 프로세스 안에서 도는 호출 횟수 제한.
> Redis 같은 외부 저장소를 쓰지 않는 이유는, 시연 규모에서 파드 하나가 기억하는 것으로
> 충분하고 의존성을 늘리고 싶지 않아서다.
> **파드가 셋이면 상한이 세 배로 느슨해지는데**, 그래도 "무제한"과 "분당 30회"는 전혀 다른 상태다.
> 파드를 가로지르는 제한은 앞단의 **WAF rate-based rule** 몫이고,
> 이 모듈은 WAF 가 없을 때의 최소 방어선이다.

**의미**

| | |
|---|---|
| 로그인 잠금 5회 | 파드 3개면 **실질 15회** |
| 파드가 재시작되면 | 카운터가 **초기화** |
| Argo CD 가 파드를 교체하면 | 마찬가지 |

**해결 방향 두 가지**

**① AWS WAF rate-based rule** — 백엔드가 지목한 방법
ALB 앞에 붙여 IP 당 요청 수를 셉니다. 파드 수와 무관합니다.
비용은 웹 ACL 월 $5 + 규칙당 $1 수준.

**② ElastiCache(Redis) 공유 카운터** — 앱 수정 필요
정확하지만 의존성이 늘고 비용도 더 듭니다.

**발표에서는** "시연 규모에서는 파드별 제한으로 충분하고,
운영에서는 WAF 로 올린다"는 설계 판단으로 설명하면 됩니다.

### 🔴 `X-Forwarded-For` 첫 값을 쓰는 것

코드가 이렇게 되어 있습니다.

```python
forwarded = request.headers.get("x-forwarded-for", "")
first = forwarded.split(",", 1)[0].strip()
```

**ALB 는 클라이언트가 보낸 XFF 뒤에 실제 IP 를 붙입니다.**
그래서 클라이언트가 헤더를 직접 넣으면 **첫 값이 조작 가능**합니다.

```
클라이언트가 보냄:  X-Forwarded-For: 1.2.3.4
ALB 통과 후:        X-Forwarded-For: 1.2.3.4, <실제IP>
                                     ↑ 앱이 이걸 씀
```

**대응 방법**

| 방법 | 설명 |
|---|---|
| **ALB 속성 변경** | `routing.http.xff_header_processing.mode = replace` — ALB 가 헤더를 덮어씀 |
| 뒤에서 세기 | 앱이 마지막 값을 쓰도록 수정 (백엔드 몫) |
| **WAF** | 어차피 WAF 를 붙이면 이 문제가 줄어듦 |

**우리 쪽에서 할 수 있는 건 ALB 속성**입니다. Terraform 이나 Ingress 어노테이션으로 설정합니다.

> 백엔드에 알려주는 게 맞습니다. 코드 주석에 *"앞에 프록시가 있어서 헤더를 덮어쓰는 것이
> 보장될 때만 켠다"* 고 되어 있는데, **ALB 기본 동작은 덮어쓰기가 아니라 append** 입니다.

---

## 6. 적용 순서

```
① A 가 values.yaml 에 11개 키 추가
② A 가 values-vagrant / values-aws 에 환경별 분기 3건
③ 이미지 재빌드 (코드가 바뀌었으므로)
④ helm upgrade
⑤ 동작 확인 (아래)
```

### 확인 방법

**속도 제한이 걸리는지**

```bash
# 로그인 6번 실패 → 6번째부터 429
for i in $(seq 1 6); do
  curl -s -o /dev/null -w "%{http_code} " -X POST \
    http://192.168.56.11:30080/api/auth/login \
    -H 'Content-Type: application/json' \
    -d '{"username":"admin","password":"wrong"}'
done ; echo
# 기대: 401 401 401 401 401 429
```

**환경변수가 들어갔는지**

```bash
kubectl exec -n reverdi deploy/reverdi-web -- printenv | grep -E "LOGIN_|CLICK_|LIVE_|TRUST_|SSL"
```

**DB 가 TLS 로 붙는지**

```bash
kubectl exec -n reverdi reverdi-db-1 -- \
  psql -U postgres -d reverdi -c \
  "SELECT ssl, client_addr FROM pg_stat_ssl JOIN pg_stat_activity USING (pid) WHERE datname='reverdi';"
```

---

## 7. 정리

| 항목 | 담당 | 시급도 |
|---|:--:|:--:|
| `values.yaml` 에 11개 키 추가 | A | 보통 |
| `TRUST_PROXY_HEADERS` 환경별 분기 | A | 🔴 **높음** |
| `DATABASE_SSL_MODE` AWS 는 `require` | A | 🔴 **높음** |
| RDS CA 번들 마운트 | C | 낮음 (AWS 이관 시) |
| ALB XFF 처리 모드 | C | 🔴 AWS 이관 시 **필수** |
| WAF rate-based rule | C | AWS 이관 시 검토 |

**로컬 검증에는 지장이 없습니다.** 기본값으로도 동작합니다.
다만 **AWS 이관 전에 `TRUST_PROXY_HEADERS` 와 `DATABASE_SSL_MODE` 는 반드시** 정리해야 합니다.
