# Vagrant 구축 테스트 — 팀 공유

> Vagrant 설치부터 앱 배포·검증까지의 작업 내역과, 팀원이 클러스터에 접속하는 방법입니다.
> 작성 기준: 2026-09-03

---

## 0. 세 줄 요약

- **VM 6대에 k3s 클러스터를 세우고 Reverdi 앱을 배포**했습니다. 웹 접속·DB 페일오버·크롤링까지 동작합니다.
- **구축 중 8건의 문제**를 만났고, 그중 3건은 우리 차트·스크립트의 실수라 **코드를 고쳤습니다.**
- 백엔드가 **읽기/쓰기 분리·S3·`/metrics`·클릭집계를 구현**해서 차트에 반영했습니다.

---

## 1. 담당별 작업 내역

문서 ⑤ 의 A/B/C 분배 기준입니다.

### 👤 A — 차트 뼈대 + 웹 (8개)

| 파일 | 상태 | 비고 |
|---|:--:|---|
| `Chart.yaml` | ✅ | |
| `values.yaml` | ✅ | **환경변수 15개 추가** (백엔드 최신 반영) |
| `templates/_helpers.tpl` | ✅ | |
| `templates/configmap.yaml` | ✅ | |
| `templates/secret.yaml` | ✅ | 훅 우선순위 부여 |
| `templates/deployment.yaml` | ✅ | 🔴 **`matchLabelKeys` 추가** (분산 문제 해결) |
| `templates/service.yaml` | ✅ | |
| `templates/ingress.yaml` | ✅ | host 분기 버그 수정 |

### 👤 B — 배치 워크로드 + 값 파일 (7개)

| 파일 | 상태 | 검토 후 수정한 것 |
|---|:--:|---|
| `templates/migrate-job.yaml` | ✅ | 🔴 **ConfigMap 을 `optional`로** (훅 순서 문제) |
| `templates/crawler-cronjob.yaml` | ✅ | 🔴 **`/dev/shm` 마운트 추가** (원본에 없었음) |
| `templates/aggregate-cronjob.yaml` | ⏸ | `app.aggregate` 미구현 → `enabled: false` |
| `templates/pgdump-cronjob.yaml` | ⏸ | 업로드 진입점 미구현 → `enabled: false` |
| `templates/pdb.yaml` | ✅ | **원본이 0바이트 빈 파일이라 새로 작성** |
| `templates/hpa.yaml` | ✅ | |
| `values-vagrant.yaml` · `values-aws.yaml` | ✅ | 환경별 기동 가드 설정 추가 |

**B 파일에서 고친 것 요약**

| 항목 | 원래 | 고침 |
|---|---|---|
| `crawler.command` | `python -m crawler.main` — **없는 모듈** | `python -m app.crawler --once` |
| `pg_dump` 이미지 | `postgres:15-alpine` | **`17-alpine`** — RDS 가 PG17 |
| 키 이름 | `backup.*` | `pgdump.*` 로 통일 |
| 이미지 이름 | `reluxe-web` | **`reluxe-backend`** — web 은 nginx 이미지 |

### 👤 C — 인프라 + CI/CD (7개 + Jenkinsfile)

| 파일 | 상태 | 검토 후 수정한 것 |
|---|:--:|---|
| `infra/registry.yaml` | ✅ | 🔴 **toleration 추가** (없으면 파드가 안 뜸) |
| `infra/registries.yaml` | ✅ | |
| `infra/postgres-cluster.yaml` | ✅ | PG **16.4 → 17.2** (AWS RDS 와 정렬) |
| `infra/minio.yaml` | ⏸ | toleration 추가. 아직 미배포 |
| `argocd/application.yaml` | ⏸ | 아직 미배포 |
| `helm-values/argocd.yaml` | ⏸ | |
| `helm-values/jenkins.yaml` | ⏸ | 🔴 **컨테이너 3개 구조로 변경** |
| `Jenkinsfile` | ⏸ | 🔴 **대폭 수정** — 아래 참조 |

**Jenkinsfile 에서 고친 것**

| 문제 | 왜 실패하는가 | 고침 |
|---|---|---|
| 한 컨테이너에 모든 도구 | `buildah` 이미지에 `uv`·`helm`·`git` 없음 → `command not found` | **컨테이너 3개** (python·buildah·tools) |
| `test` 가 `docker run` 으로 DB | k3s 는 containerd — **Docker 데몬이 없음** | Kubernetes 플러그인 **사이드카** |
| `IMAGE_TAG` 가 null | `environment` 블록은 **체크아웃 전**에 평가됨 | `script` 블록으로 이동 |
| `alpine/helm` 에 git 없음 | Alpine 은 git 기본 미포함 | `apk add --no-cache git` |
| `post` 가 오류를 가림 | 앞 단계 실패 시 컨테이너가 없어 정리도 실패 | `container()` 래핑 제거 |
| `sleep 5` 로 DB 대기 | 느린 노드에서 부족 | 접속 확인 루프(최대 60초) |

**C 담당은 아직 배포 전입니다.** Jenkins·Argo CD·MinIO 는 다음 단계입니다.

---

## 2. 구축 중 겪은 문제 8건

### 우리 코드의 실수 (3건) — 고쳤습니다

| # | 증상 | 원인 | 고친 파일 |
|:--:|---|---|---|
| 1 | 웹훅 타임아웃 · 노드 간 파드 통신 불가 | **Flannel 이 Vagrant NAT 인터페이스를 잡음** | `k3s-server.sh` · `k3s-agent.sh` |
| 2 | `configmap "reluxe-config" not found` | Helm 훅이 일반 리소스보다 먼저 실행됨 | `migrate-job.yaml` |
| 3 | 배포 후 파드가 한 노드에 몰림 | `topologySpread` 가 **옛 세대 파드까지 계산** | `deployment.yaml` |

### 환경 문제 (5건) — 대응 방법을 문서화했습니다

| # | 증상 | 원인 | 대응 |
|:--:|---|---|---|
| 4 | 박스 다운로드 404 | Rocky 9.6 이 Vault 로 이전, 레지스트리 미갱신 | `bento/rockylinux-9` |
| 5 | VM 부팅 타임아웃 | 디스크 리사이즈 + `cpuexecutioncap` | `boot_timeout 900` · cap 제거 |
| 6 | `set: pipefail\r: invalid option` | PowerShell CRLF | `sed -i 's/\r$//'` |
| 7 | `tee: No such file or directory` | 에이전트에 `/etc/rancher/k3s` 없음 | `mkdir -p` (common.sh) |
| 8 | `secret "reluxe-db-app" not found` | `bootstrap.initdb.secret` 명시 시 자동생성 안 함 | 직접 생성 |

### 🔴 문제 1 — 가장 오래 걸린 것

```bash
$ ip -d link show flannel.1
vxlan id 1 local 10.0.2.15 dev enp0s3      # ← 6대 전부 같은 IP
```

VM 에 NIC 이 두 개인데(`enp0s3` = Vagrant NAT, `enp0s8` = 사설망),
**Flannel 이 NAT 쪽을 잡았습니다.**

`--node-ip` 를 줬는데도 이렇게 된 이유 — 그건 "쿠버네티스가 보는 노드 주소"만 바꿉니다.
**CNI 가 쓰는 인터페이스는 별개**라 `--flannel-iface` 를 따로 줘야 합니다.

증상이 "파드는 뜨는데 노드 간 통신만 안 됨"이라, 방화벽·라우팅을 다 뒤져도 안 나옵니다.

### 🔴 문제 3 — 설정은 맞는데 동작이 안 된 사례

```
reluxe-web-...-6zbgh   node1
reluxe-web-...-9kljz   node1     ← 2개가 몰림
reluxe-web-...-rbf77   node2
                       node3 은 비어 있음
```

`topologySpreadConstraints` 를 넣었고 `DoNotSchedule` 로 바꿔도 계속 몰렸습니다.

원인은 `labelSelector` 가 **롤링 업데이트 중 옛 파드까지 세는 것**이었습니다.
스케줄러가 "node3 은 이미 찼다"고 판단하는데, 그 파드는 곧 사라집니다.

```yaml
matchLabelKeys:
  - pod-template-hash      # 같은 세대끼리만 계산 (k8s 1.27+)
```

이 한 줄로 해결됐습니다. **규칙 위반이 아니라 세는 대상이 틀렸던 것**입니다.

---

## 3. 백엔드 최신 반영

`백엔드_수정요청.txt` 항목이 대부분 구현되었습니다.

| 요청 | 상태 |
|:--:|---|
| 1. 읽기/쓰기 세션 분리 | ✅ `DATABASE_RO_URL` · 폴백 쿨다운까지 |
| 2. `SESSION_SECRET` 기동 거부 | ✅ `APP_ENV=production` 가드 |
| 3. 쿠키 `secure` | ✅ `COOKIE_SECURE` |
| 4. `/metrics` | ✅ `prometheus-fastapi-instrumentator` |
| 5. `--proxy-headers` | ✅ 원래 있었음 |
| 6. 클릭 이벤트 | ✅ `POST /api/events/click` |

**차트에 환경변수 15개를 추가**했습니다. 특히 `APP_ENV` 가 중요합니다.

| 환경 | `APP_ENV` | 동작 |
|---|---|---|
| Vagrant | `local` | 가드 off — S3 없이도 뜸 |
| AWS | `production` | 가드 on — **설정 누락 시 기동 거부** |

> ⚠️ AWS 배포 전에 `values-aws.yaml` 의 `<S3_BUCKET_NAME>` 을 채워야 합니다.
> 안 채우면 파드가 `CrashLoopBackOff` 로 안 뜹니다. (의도된 동작)

---

## 4. 검증 결과

| 항목 | 결과 |
|---|---|
| 노드 6대 | `Ready` · IP 각자 다름 |
| taint 격리 | node0 컨트롤 · node4 batch · node5 infra |
| **웹 파드 분산** | ✅ node1·2·3 에 **하나씩** |
| **DB 고가용성** | ✅ primary + replica 2, 다른 노드에 |
| **DB 페일오버** | ✅ `reluxe-db-1` → `reluxe-db-2` 자동 승격 |
| **무중단** | ✅ 페일오버 중 **웹 응답 200 연속 유지** |
| 마이그레이션 순서 | ✅ Helm 훅으로 앱보다 먼저 |
| 읽기/쓰기 분리 | ✅ 로그에 `reluxe-db-ro` 연결 확인 |
| `/metrics` | ✅ Prometheus 지표 노출 |
| **크롤러** | ✅ node4 에서 Playwright + Chromium 동작 |
| **`/dev/shm`** | ✅ `tmpfs 1.0G` 마운트 확인 |
| 중복 수집 방지 | ✅ "다른 프로세스가 수집 중" |
| 무중단 롤링 업데이트 | ✅ 새 파드 Ready 후 옛 파드 제거 |

### 페일오버 시연 (발표 영상 B 확보)

```
09:37:40  200
09:37:41  200        ← primary 삭제
   ...             ← 자동 승격 진행
09:38:08  200        끊김 0회
```

`PRIMARY: reluxe-db-1 → reluxe-db-2` · 웹 파드 `RESTARTS 0`

**AWS RDS Multi-AZ 와 같은 동작**입니다. 앱 코드는 그대로 가고, Secret 의 접속 주소만 바뀝니다.

---

## 5. 🔴 팀원이 클러스터에 접속하는 방법

### 먼저 알아야 할 제약

VirtualBox **호스트 전용 네트워크(192.168.56.0/24)는 VM 을 띄운 PC 안에서만** 보입니다.
다른 PC 에서는 그 IP 로 접근할 수 없습니다.

### 방법 A — 화면 공유 (가장 확실)

Zoom · Teams · 디스코드로 화면을 공유합니다.

- 네트워크 설정이 필요 없습니다
- 팀원은 **보기만** 가능합니다
- **발표 리허설·시연 확인에 적합**합니다

### 방법 B — 포트 포워딩

`Vagrantfile` 에 추가합니다.

```ruby
node.vm.network "forwarded_port", guest: 30080, host: 30080, host_ip: "0.0.0.0"
```

> `host_ip: "0.0.0.0"` 이 중요합니다. 기본값(`127.0.0.1`)이면 그 PC 안에서만 열립니다.

```powershell
vagrant reload node1
```

팀원은 이렇게 접속합니다.

```
http://<VM 띄운 PC 의 LAN IP>:30080
```

**PC 의 LAN IP 확인**

```powershell
ipconfig | findstr IPv4
```

⚠️ **윈도우 방화벽에서 그 포트를 허용**해야 합니다.

```powershell
# 관리자 PowerShell
New-NetFirewallRule -DisplayName "Reverdi 30080" -Direction Inbound -LocalPort 30080 -Protocol TCP -Action Allow
```

> 학원 PC 는 정책상 막혀 있을 수 있습니다. 안 되면 방법 A 나 D 로 가세요.

### 방법 C — 브리지 네트워크

VM 을 공유기에 직접 붙여 LAN IP 를 받게 합니다.

```ruby
node.vm.network "public_network", bridge: "이더넷"
```

VM 이 학원 네트워크의 IP 를 직접 받으므로 팀원이 바로 접근할 수 있습니다.

⚠️ 학원 네트워크가 **임의 장치에 DHCP 를 주는지**에 달려 있습니다.
보안 정책상 막힌 경우가 많습니다.

### 방법 D — 각자 자기 PC 에 구축 (권장)

`vagrant-setup.zip` 의 `Vagrantfile` 과 스크립트만 있으면 **누구나 같은 환경을 재현**할 수 있습니다.

```powershell
vagrant plugin install vagrant-disksize
cd vagrant
vagrant up
```

**RAM 요구량**

| 구성 | RAM |
|---|---|
| 6 VM 풀 구성 | **40GB** |
| 축소 4 VM (node0~3) | **16GB** |

RAM 이 부족하면 `Vagrantfile` 의 `NODES` 배열에서 node4·node5 줄을 지우면 됩니다.
그 경우 크롤러·레지스트리는 못 돌리지만, **웹 배포와 DB 페일오버는 확인 가능**합니다.

> 이게 가장 좋은 방법입니다. **각자 자기 것을 부수고 다시 만들 수 있어야** 학습이 됩니다.

### 방법 E — kubeconfig 공유 (클러스터 조작용)

웹 화면이 아니라 `kubectl` 을 쓰고 싶다면, 방법 B·C 로 **6443 포트**를 연 뒤 kubeconfig 를 전달합니다.

```bash
# node0 에서
sudo cat /etc/rancher/k3s/k3s.yaml
```

받은 파일에서 `server:` 주소를 접근 가능한 IP 로 바꾸고, 팀원 PC 의 `~/.kube/config` 에 둡니다.

⚠️ **이 파일에는 클러스터 관리자 권한이 들어 있습니다.** 아무 데나 올리지 마세요.

---

## 6. 남은 작업

| 항목 | 담당 | 비고 |
|---|:--:|---|
| Jenkins 배포 | C | `helm-values/jenkins.yaml` 준비됨 |
| Argo CD 배포 | C | `argocd/application.yaml` 준비됨 |
| MinIO 배포 | C | 백업 업로드 대상 |
| 클릭 집계 CronJob | B | `app.aggregate` 미구현 — SQS 도입 시점에 |
| pg_dump CronJob | B | 업로드 진입점 미구현 |
| 웹 시연 영상 (A) | 전원 | 크롤링 데이터가 들어왔으니 촬영 가능 |
| AWS `<S3_BUCKET_NAME>` 채우기 | — | 배포 전 필수 |

---

## 7. 🔴 커밋하면 안 되는 것

Secret 값은 **`kubectl create` 로만** 만듭니다.

```
reluxe-db-app    DB 계정
reluxe-secret    앱 환경변수 (DATABASE_URL · SESSION_SECRET · 로그인 계정)
minio-credentials
```

`.gitignore` 에 `helm.exe` · `out*.yaml` · `*.key` · `kubeconfig` 를 넣어뒀습니다.
`git status` 로 한 번 확인한 뒤 커밋하세요.
