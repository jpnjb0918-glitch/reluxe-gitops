# 백엔드에 확인·요청할 것

> `docs/security.md` 를 읽고 인프라 쪽에서 확인한 결과입니다.
> **새로 요청할 것은 없고, 확인 하나와 공유 두 건**입니다.

---

## ❓ 확인 요청 1건

### `secretScope.perWorkload` 를 켜도 되는 이미지인가

`docs/security.md` 에 이렇게 적혀 있습니다.

> gitops `secretScope.perWorkload: true` — **4번 패치가 머지된 이미지에서만.**
> 이전 이미지는 크롤러가 관리자 비밀번호 없이는 안 뜬다

**우리 쪽 준비는 끝났습니다.** 차트에 스위치를 만들었고 기본은 꺼둔 상태입니다.

**확인하고 싶은 것**

| | |
|---|---|
| 4번 패치가 들어간 커밋 | `9af5e14` 이후로 보는 게 맞나요? |
| 확인 방법 | 아래 명령으로 판별해도 되나요? |

```bash
kubectl exec -n reverdi deploy/reverdi-web -- \
  python -c "from app.config import require_secrets; print('ok')"
```

`ImportError` 가 나면 옛 이미지로 보고 재빌드하려 합니다.
**더 확실한 판별 방법이 있으면 알려주세요.**

---

## ✅ 앱 쪽 요청은 없습니다

제가 처음에 두 가지를 요청하려 했는데, **`docs/security.md` 를 읽고 철회합니다.**

### ① `/docs` 끄기 — 철회

> `/docs`·`/openapi.json` 은 앱에서 안 막는다. **저장소가 공개라 API 목록은 어차피 보인다.**
> 실질 가치는 `/metrics`(실시간 트래픽·에러율) 차단이고 그건 ALB 고정 404 로.

**타당합니다.** 저장소가 public 이면 `/docs` 를 막아도 소스에서 API 를 다 볼 수 있습니다.
막을 가치가 있는 건 실시간 운영 정보이고, **ALB 에서 처리하겠습니다.**

### ② X-Forwarded-For 뒤에서 세기 — 철회

> `TRUST_PROXY_HEADERS`(운영 자동)가 켜져야 ALB 뒤에서 X-Forwarded-For 로 사용자를 구분한다;
> **로컬에서는 꺼져 있어야 헤더 한 줄로 못 피한다.**

의도가 명확합니다. 다만 **ALB 기본 동작이 append** 라 첫 값이 조작 가능합니다.

```
클라이언트:  X-Forwarded-For: 1.2.3.4
ALB 통과 후: X-Forwarded-For: 1.2.3.4, <실제IP>
                              ↑ 앱이 이걸 쓴다
```

**앱을 고치는 대신 ALB 속성으로 해결하겠습니다.**

```
routing.http.xff_header_processing.mode = replace
```

ALB 가 헤더를 덮어쓰게 하면 첫 값이 곧 실제 IP 가 됩니다.
**앱 수정 불필요합니다.**

> 혹시 이 방식에 문제가 있다고 보시면 알려주세요.

---

## 📌 공유 — 인프라에서 처리하는 것

`docs/security.md` 의 "인프라에 넘긴 것" 4건입니다. 순서 그대로 진행합니다.

| # | 항목 | 상태 |
|:--:|---|---|
| 1 | RDS `rds.force_ssl=1` + `DATABASE_SSL_MODE=require` | 🟡 앱 설정 완료 · RDS 미적용 |
| 2 | `secretScope.perWorkload: true` | ✅ **차트 준비 완료** · 이미지 확인 후 |
| 3 | WAF rate-based + ALB 고정 404 | 🔴 AWS 이관 시 |
| 4 | S3 비공개 · CloudFront · 예산 · 복원 리허설 | 🔴 AWS 이관 시 |

### 2번 — 차트에 반영한 내용

```
reverdi.envFrom        웹     — reverdi-secret (전체)
reverdi.envFromBatch   배치   — reverdi-db-secret (DB만)
```

| 워크로드 | 받는 것 |
|---|---|
| 웹 | 계정 · SESSION_SECRET · DB |
| 크롤러 · 집계 · 백업 · 마이그레이션 | **DB 접속만** |

**근거를 코드에서 확인했습니다.**

```bash
grep -rn "from app.auth" app/crawler/   # 없음
grep -rn "from app.auth" alembic/       # 없음
```

### 3번 — ALB 고정 404 대상

지정하신 경로 그대로 적용합니다.

```
/metrics   /docs   /openapi.json   /ready
```

`/health` 는 ALB 헬스체크에 쓰이므로 열어둡니다.

---

## 💡 `docs/security.md` 잘 봤습니다

**"남긴 것"** 섹션이 특히 유용합니다.

> 발표에서 "그럼 완벽하냐"는 질문에 **남긴 것을 먼저 말하는** 것이 이 문서의 용도다

발표 준비에 그대로 쓰겠습니다. **"알고 있으며 우선순위의 문제"** 라고 답할 수 있는 게 강합니다.

호출 제한이 파드 단위라는 것도 저희가 실측으로 확인했습니다.

```
설정 5회 → 실제 12회쯤에서 429   (파드 3개라 각자 셈)
```

**WAF 로 올려야 하는 근거를 숫자로 보여줄 수 있게** 됐습니다.
