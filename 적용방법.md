# 적용 방법

깨진 파일을 정리하고 이 패키지로 교체하는 순서입니다.

---

## 1. 먼저 VM 수정본 받기

```powershell
cd D:\project\Reluxe
git pull
```

`argocd/application.yaml` · `infra/postgres-cluster.yaml` 이 정상으로 바뀝니다.

---

## 2. 깨진 파일 삭제

```powershell
cd D:\project\Reluxe

Remove-Item -Recurse -Force vagrant
Remove-Item "2026_09_02_Vagrant_구축테스트_작업기록.md"
Remove-Item "수정내역.md"
```

| 삭제 | 대체 |
|---|---|
| `vagrant/` | 이 패키지의 `vagrant/` (전자동 버전) |
| `2026_09_02_..._작업기록.md` | `문제해결_기록.md` |
| `수정내역.md` | 내용이 위 문서에 흡수됨 |

---

## 3. 이 패키지 내용을 복사

🔴 **압축 해제는 7-Zip 이나 반디집으로 하세요.**
윈도우 기본 압축 해제가 인코딩을 깨뜨린 것으로 보입니다.

```
vagrant/                     →  D:\project\Reluxe\vagrant\
문제해결_기록.md              →  D:\project\Reluxe\
구축테스트_과정기록.md         →  D:\project\Reluxe\
팀공유_구축테스트정리.md       →  D:\project\Reluxe\
```

---

## 4. 확인

```powershell
cd D:\project\Reluxe
Get-Content vagrant\Vagrantfile -Encoding UTF8 -TotalCount 15
```

한글이 정상으로 보여야 합니다.

**전체 검사**

```powershell
Get-ChildItem -Recurse -Include *.yaml,*.tpl,Vagrantfile,*.sh,*.md -File |
  Where-Object { $_.FullName -notmatch '\\\.git\\' } |
  ForEach-Object {
    $c = Get-Content $_.FullName -Raw -Encoding UTF8
    if ($c -match '[濡쒖뺄겢윭뒪꽣]|\?\?\?') { Write-Host "🔴 $($_.FullName)" }
  }
```

**아무것도 안 나오면** 정리 완료입니다.

---

## 5. 커밋

```powershell
git add -A
git status
git commit -m "chore: 인코딩 깨진 파일 정리, 전자동 구축 스크립트 추가"
git push
```

---

## 6. 🔴 앞으로 이런 일을 막으려면

### PowerShell 로 한글 파일을 일괄 치환하지 마세요

`Get-Content` / `Set-Content` 는 기본 인코딩이 UTF-8 이 아닙니다.
읽을 때 CP949 로 해석해버려 한글이 깨집니다.

이번에 파일 7개가 이렇게 망가졌습니다.

```
원본 UTF-8  →  CP949 로 잘못 읽음  →  UTF-8 로 저장
```

이 과정에서 CP949 에 없는 문자가 `?` 로 바뀌어 **정보가 사라집니다.**
되돌릴 수 없어 다시 만들어야 했습니다.

### 대신 VS Code 를 쓰세요

```
Ctrl+Shift+H  →  폴더 전체에서 바꾸기
```

인코딩을 건드리지 않습니다.

### 꼭 PowerShell 로 해야 한다면

```powershell
$c = [System.IO.File]::ReadAllText($path, [System.Text.Encoding]::UTF8)
$c = $c -replace 'before', 'after'
[System.IO.File]::WriteAllText($path, $c, (New-Object System.Text.UTF8Encoding $false))
```

`$false` 가 **BOM 없는 UTF-8** 입니다. BOM 이 붙으면 YAML 파서가 거부합니다.

---

## 7. 이 패키지에 든 것

| 파일 | 내용 |
|---|---|
| `vagrant/` | **전자동 구축** — `vagrant up` 한 번으로 클러스터 전체 |
| `vagrant/README.md` | 사용법 · 접속 정보 확인법 · 문제 해결 |
| `문제해결_기록.md` | 겪은 문제 16건의 증상 · 원인 · 해결 · 배운 것 |
| `구축테스트_과정기록.md` | 구성 · 검증 결과 · AWS 이관 계획 |
| `팀공유_구축테스트정리.md` | 담당별 작업 내역 · 팀원 접속 방법 |

### ⚠️ `vagrant/` 는 아직 실행 검증 전입니다

문법 · 순서 · 포트 충돌은 확인했지만,
**`vagrant up` 을 처음부터 끝까지 돌려본 적은 없습니다.**

팀에 공유하기 전에 한 번 검증하시길 권합니다.

```powershell
cd vagrant
vagrant up 2>&1 | Tee-Object -FilePath build.log
```

`Tee-Object` 로 로그를 남기면 어디서 막혔는지 나중에 볼 수 있습니다.

**막힐 가능성이 있는 곳**

| 단계 | 위험 |
|---|---|
| `20-trigger.sh` | `sshpass` 설치 (Rocky 9 기본 저장소에 없어 EPEL 필요) |
| `40-images.sh` | 크롤러 2.7GB 빌드 — 네트워크에 따라 30분 넘을 수도 |
| `90-jenkins.sh` | 플러그인 `:latest` 버전 충돌 |
| `70-argocd.sh` | 차트 키 이름이 최신 버전에서 바뀌었을 수 있음 |
