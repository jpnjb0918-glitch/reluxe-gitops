# 변경 사항 — 저장소에 반영할 것

> 2026-09-04 · 전자동 구축 실행 검증 완료 후 정리

---

## 1. 🔴 삭제

인코딩이 깨져 복구 불가능한 파일입니다. 내용은 다른 문서에 흡수됐습니다.

```
수정내역.md
2026_09_02_Vagrant_구축테스트_작업기록.md
```

---

## 2. 재작성 — 주석이 깨져 있던 YAML 2개

**값은 그대로 두고 주석만 다시 썼습니다.** 파싱은 되던 상태라 동작에는 지장이 없었습니다.

| 파일 | 확인 |
|---|---|
| `argocd/application.yaml` | `repoURL` · `path` · `syncPolicy` 그대로 |
| `infra/postgres-cluster.yaml` | `instances: 3` · `postgresql:17.2` · `podAntiAffinity` 그대로 |

`postgres-cluster.yaml` 주석에 **문제 12(CNPG Secret 덮어쓰기)의 적용 순서**를 적어뒀습니다.

---

## 3. 문서 갱신

| 파일 | 변경 |
|---|---|
| `문제해결_기록.md` | 16건 → **18건** (17 · 18번 추가) |
| `팀공유_구축테스트정리.md` | 최신본으로 교체 (8건짜리 옛 버전이었음) |
| `적용방법.md` | "검증 전" → **"검증 완료 (90분)"** |
| `vagrant/README.md` | 검증 배지 추가 |

### 추가된 문제 2건

**17. root PATH 에 `/usr/local/bin` 이 없음 — 재발**

```
node0: /tmp/scripts/10-k3s-server.sh: line 68: kubectl: command not found
```

문제 5 와 같은 원인인데, **문서에만 적고 코드에 안 옮겨서** 자동화에서 재발했습니다.

> 재발 방지는 문서가 아니라 코드에 넣어야 합니다.

**18. node-exporter 가 node0 에만 안 뜸**

`operator: Exists` 에 **`effect` 를 명시하면 그 effect 만** 견딥니다.
node0 의 taint 는 `NoExecute` 라 `NoSchedule` 만으로는 통과 못 합니다.

```yaml
tolerations:
  - { operator: Exists, effect: NoSchedule }
  - { operator: Exists, effect: NoExecute }
```

---

## 4. 추가 — 발표용 도표

| 파일 | 내용 |
|---|---|
| `구축테스트_정리.drawio` | 3장 — 최종 구성 · 검증 결과 · CI/CD 흐름 |
| `AWS구성도_발표용.drawio` | 4장 — **16:9 (1600×900)** 슬라이드 비율 |

기존 `AWS_구성도_최종.drawio` 는 1페이지가 1900×2870 이라 PPT 에 넣으면
글자가 읽히지 않습니다. **요약본을 따로** 만들었습니다.

**PPT 에 넣는 법**
```
파일 → 내보내기 형식 → PNG
  확대/축소  200%   ·   테두리 10   ·   투명 배경 해제
```

원본은 **설계 문서로 남겨두세요.** CIDR · 인스턴스 타입 · 비용 계산이 들어 있어
질문이 나올 때 근거로 쓰입니다.

---

## 5. 적용

```powershell
cd D:\project\Reluxe

# 삭제
Remove-Item "수정내역.md", "2026_09_02_Vagrant_구축테스트_작업기록.md" -ErrorAction SilentlyContinue

# 이 패키지 내용을 덮어쓰기 (7-Zip · 반디집으로 압축 해제)

git add -A
git status
git commit -m "docs: 검증 완료 반영, 깨진 파일 정리, 발표용 도표 추가"
git push
```

### 확인

```powershell
Get-ChildItem -Recurse -Include *.yaml,*.sh,*.md,Vagrantfile -File |
  Where-Object { $_.FullName -notmatch '\\\.git\\' -and $_.Name -ne '적용방법.md' -and $_.Name -ne '변경사항.md' } |
  ForEach-Object {
    $c = Get-Content $_.FullName -Raw -Encoding UTF8
    if ($c -match '[濡쒖뺄겢윭뒪꽣]') { Write-Host "확인필요: $($_.Name)" }
  }
```

아무것도 안 나오면 정리 완료입니다.

---

## 6. ⚠️ 인코딩이 계속 깨지는 이유

**PowerShell 의 `Get-Content` / `Set-Content` 는 기본 인코딩이 UTF-8 이 아닙니다.**

```
원본 UTF-8  →  CP949 로 잘못 읽음  →  UTF-8 로 저장
```

이 과정에서 CP949 에 없는 문자가 `?` 로 바뀌어 **정보가 사라집니다.**
되돌릴 수 없어 파일을 다시 만들어야 합니다.

이번까지 **두 차례** 이 문제로 파일을 다시 만들었습니다.

### 대신 VS Code 를 쓰세요

```
Ctrl+Shift+H  →  폴더 전체에서 바꾸기
```

### 꼭 PowerShell 로 해야 한다면

```powershell
$p = (Resolve-Path "경로\파일.yaml").Path      # 🔴 .NET 은 절대 경로가 필요하다
$c = [System.IO.File]::ReadAllText($p, [System.Text.Encoding]::UTF8)
$c = $c -replace 'before', 'after'
[System.IO.File]::WriteAllText($p, $c, (New-Object System.Text.UTF8Encoding $false))
```

`$false` 가 **BOM 없는 UTF-8** 입니다.
