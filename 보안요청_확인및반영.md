# 보안 요청 4건 — 확인 및 반영

> 백엔드가 `docs/security.md` 로 정리한 내용을 읽고 확인했습니다.
> **인프라 쪽에서 할 일**과 **차트에 반영한 것**을 나눴습니다.

---

## 0. 먼저 — 제 이전 분석에 틀린 게 있었습니다

**"프로그램별 열쇠 분리"를 IRSA(파드별 IAM 역할)로 해석했는데, 아니었습니다.**

실제로는 **Secret 범위 분리**입니다. `docs/security.md` 의 4번 항목입니다.

> `config.py` 가 임포트 시점에 관리자 비밀번호를 요구해서 **크롤러도 받아야 떴다.**
> 요구를 그 값을 실제로 쓰는 `app/auth.py`(웹만 임포트)의 `require_secrets()` 로 옮김.
> 크롤러·집계·백업은 `DATABASE_URL` 만으로 기동

**앱 쪽은 이미 고쳐져 있고, 우리가 Secret 을 나눠 주면 됩니다.**

---

## 1. 요청 4건 상태

| # | 요청 | 상태 |
|:--:|---|---|
| 1 | RDS `force_ssl` + `DATABASE_SSL_MODE=require` | 🟡 앱 완료 · **RDS 미적용** |
| 2 | 프로그램별 열쇠 분리 | ✅ **차트에 반영** (이번에) |
| 3 | WAF — 횟수 제한 + 내부 화면 차단 | 🔴 미적용 |
| 4 | S3 비공개 · CloudFront · 예산 · 복원 리허설 | 🔴 미적용 |

**로컬 검증에는 영향 없습니다.** 전부 AWS 이관 시점입니다.

---

## 2. ✅ 반영한 것 — 비밀값 범위 분리

### 무엇이 문제였나

지금까지 **크롤러도 웹과 같은 Secret 을 통째로** 받았습니다.

```yaml
# 전 워크로드가 이걸 썼다
envFrom:
  - secretRef:
      name: reverdi-secret     # ADMIN_PASSWORD · SESSION_SECRET 포함
```

크롤러 하나가 뚫리면 **관리자 비밀번호까지 넘어갑니다.**

### 근거 — 실제로 확인했습니다

```bash
$ grep -rn "from app.auth" app/crawler/     # 결과 없음
$ grep -rn "from app.auth" alembic/         # 결과 없음
```

크롤러도 마이그레이션도 `app/auth.py` 를 임포트하지 않습니다.
**관리자 비밀번호가 필요 없습니다.**

### 어떻게 고쳤나

`_helpers.tpl` 에 배치용 헬퍼를 추가했습니다.

```
reverdi.envFrom        웹     — 전체 Secret
reverdi.envFromBatch   배치   — DB 접속만
```

| 워크로드 | 받는 Secret |
|---|---|
| `deployment` (웹) | `reverdi-secret` |
| `crawler-cronjob` | `reverdi-db-secret` |
| `aggregate-cronjob` | `reverdi-db-secret` |
| `pgdump-cronjob` | `reverdi-db-secret` |
| `migrate-job` | `reverdi-db-secret` |

### 🔴 기본값은 꺼둔 상태입니다

```yaml
secretScope:
  perWorkload: false          # 켜기 전에 이미지 확인
  dbSecretName: reverdi-db-secret
```

`false` 면 **기존과 똑같이** 동작합니다. 지금 클러스터에 영향 없습니다.

### 켜는 순서

백엔드가 순서를 명시했습니다.

> gitops `secretScope.perWorkload: true` — **4번 패치가 머지된 이미지에서만.**
> 이전 이미지는 크롤러가 관리자 비밀번호 없이는 안 뜬다

**① 이미지가 최신인지 확인**

```bash
kubectl exec -n reverdi deploy/reverdi-web -- \
  python -c "from app.config import require_secrets; print('4번 패치 있음')"
```

`ImportError` 가 나면 옛 이미지입니다. **먼저 다시 빌드하세요.**

**② DB 전용 Secret 생성**

```bash
kubectl create secret generic reverdi-db-secret -n reverdi \
  --from-literal=DATABASE_URL="$(kubectl get secret reverdi-secret -n reverdi \
     -o jsonpath='{.data.DATABASE_URL}' | base64 -d)" \
  --from-literal=DATABASE_RO_URL="$(kubectl get secret reverdi-secret -n reverdi \
     -o jsonpath='{.data.DATABASE_RO_URL}' | base64 -d)"
```

**③ 켜기**

```bash
helm upgrade reverdi charts/reverdi -n reverdi \
  -f charts/reverdi/values-vagrant.yaml \
  --set secretScope.perWorkload=true
```

**④ 확인**

```bash
# 크롤러에 관리자 비밀번호가 없어야 한다
kubectl create job -n reverdi --from=cronjob/reverdi-crawler scope-test
kubectl exec -n reverdi job/scope-test -- printenv | grep -c ADMIN_PASSWORD
```

**`0`** 이면 성공입니다. 그런데도 크롤러가 정상 동작하면 분리가 된 겁니다.

---

## 3. 🔴 백엔드에 요청할 것 — 없습니다

제가 앞서 `/docs` 를 끄자고 했는데, **백엔드가 이미 판단했습니다.**

> `/docs` · `/openapi.json` 은 앱에서 안 막는다. **저장소가 공개라 API 목록은 어차피 보인다.**
> 실질 가치는 `/metrics`(실시간 트래픽·에러율) 차단이고 그건 **ALB 고정 404** 로.

**타당합니다.** 저장소가 public 이면 `/docs` 를 막아도 소스에서 API 를 다 볼 수 있습니다.
막을 가치가 있는 건 **실시간 운영 정보인 `/metrics`** 고, 그건 우리 몫입니다.

### XFF 도 이미 정리돼 있습니다

> `TRUST_PROXY_HEADERS`(운영 자동)가 켜져야 ALB 뒤에서 `X-Forwarded-For` 로 사용자를 구분한다;
> **로컬에서는 꺼져 있어야 헤더 한 줄로 못 피한다.**

우리 `values-aws.yaml` 이 `true`, `values-vagrant.yaml` 이 `false` 로 이미 맞습니다.

> ⚠️ 다만 **ALB 속성**은 우리가 설정해야 합니다. 아래 4장 참조.

---

## 4. 🔴 인프라(C 담당)가 할 것

백엔드가 순서까지 정해줬습니다.

### ① RDS 암호화 강제

```
RDS 파라미터 그룹 → rds.force_ssl = 1
```

| 설정 | 하는 일 | 상태 |
|---|---|:--:|
| `DATABASE_SSL_MODE=require` (앱) | 클라이언트가 TLS 요구 | ✅ |
| `rds.force_ssl=1` (RDS) | **서버가 평문 거부** | 🔴 |

**둘 다 있어야** 완성입니다.

> `verify-full` 은 RDS CA 번들 마운트가 필요해 나중에 해도 됩니다.
> 백엔드도 "인프라 작업과 같이" 로 남겨뒀습니다.

### ② 비밀값 범위 분리

**2장 참조.** 차트는 준비됐고, **이미지 확인 후 켜면** 됩니다.

### ③ WAF

**rate-based rule**

앱 레벨 제한은 파드 수만큼 느슨해집니다. 우리가 실측했습니다.

```
설정 5회 → 실제 12회쯤에서 429   (파드 3개라 각자 셈)
```

| 대상 | 앱 설정 |
|---|---|
| 로그인 | 5회 / 300초 |
| 실시간 검색 | 10회 / 60초 |
| 클릭 | 60회 / 60초 |

**ALB 고정 404**

백엔드가 지정한 경로입니다.

```
/metrics  /docs  /openapi.json  /ready
```

> `/ready` 도 막습니다. DB 연결 상태가 드러나기 때문입니다.
> `/health` 는 ALB 헬스체크에 쓰이므로 열어둡니다.

**🔴 ALB XFF 속성**

```
routing.http.xff_header_processing.mode = replace
```

이게 없으면 클라이언트가 헤더를 위조해 **속도 제한을 우회**합니다.
ALB 기본은 append 라 첫 값이 조작 가능합니다.

### ④ S3 · 예산 · 복원

| 항목 | |
|---|---|
| S3 퍼블릭 액세스 차단 + CloudFront OAC | `S3_PUBLIC_BASE` 에 CloudFront 도메인 |
| AWS Budgets | 팀 예산 $700 기준 · 80% · 100% 알림 |
| **백업 복원 리허설** | 🔴 **발표 전 필수** |

> **복원해본 적 없는 백업은 백업이 아닙니다.**
> 스냅샷에서 새 인스턴스를 만들어 데이터를 확인하고, RTO·RPO 를 숫자로 재보세요.

---

## 5. 백엔드 문서에서 눈여겨볼 것

`docs/security.md` 의 **"남긴 것"** 이 발표에 유용합니다.

> 발표에서 "그럼 완벽하냐"는 질문에 **남긴 것을 먼저 말하는** 것이 이 문서의 용도다.

| 남긴 것 | 왜 |
|---|---|
| 호출 제한이 파드 단위 | WAF 로 올릴 몫 |
| IP 를 바꿔가며 오는 봇넷 | 앱 단독으로 불가 · WAF Bot Control |
| 소유 검사의 SELECT↔upsert 틈 | 닫으려면 크롤러 경로까지 건드림 |
| CSP 헤더 없음 | 동결 중이라 인라인 스크립트가 깨질 수 있음 |
| `verify-full` 은 옵션 | 인프라 작업과 같이 |

**"알고 있으며 우선순위의 문제"** 라고 말할 수 있는 게 강합니다.

---

## 6. 변경 파일

| 파일 | 변경 |
|---|---|
| `charts/reverdi/templates/_helpers.tpl` | `envFromBatch` 헬퍼 추가 |
| `charts/reverdi/values.yaml` | `secretScope` 블록 (기본 `false`) |
| `charts/reverdi/templates/crawler-cronjob.yaml` | `envFromBatch` 사용 |
| `charts/reverdi/templates/aggregate-cronjob.yaml` | 〃 |
| `charts/reverdi/templates/pgdump-cronjob.yaml` | 〃 |
| `charts/reverdi/templates/migrate-job.yaml` | 조건부 Secret |

**기본값이 `false` 라 지금 클러스터에 영향 없습니다.**

---

## 7. 검증

| 항목 | |
|---|:--:|
| 헬퍼 정의 ↔ 사용 일치 | ✅ |
| 워크로드별 헬퍼 배정 | ✅ |
| `.Values` 참조 전부 존재 (vagrant · aws) | ✅ |
| 템플릿 구문 균형 12개 | ✅ |
| 기본값 `perWorkload: false` | ✅ |
| 크롤러·alembic 이 `app/auth.py` 미임포트 | ✅ **실제 코드 확인** |
