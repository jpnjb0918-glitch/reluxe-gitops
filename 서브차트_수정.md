# 서브차트 키 수정 — 팀원 지적 반영

> 팀원이 `nodeExporter` → `prometheus-node-exporter` 로 바꾸니 됐다고 알려준 건입니다.
> **확인 결과 맞습니다.** 수정하고 다른 곳도 함께 점검했습니다.

---

## 무엇이 문제였나

`kube-prometheus-stack` 은 node-exporter 를 직접 만들지 않고
**`prometheus-node-exporter` 라는 별도 차트를 가져다 씁니다.**

서브차트에 값을 넘기려면 **서브차트 이름을 키로** 써야 합니다.

| 키 | 누가 읽나 |
|---|---|
| `nodeExporter.enabled` | 상위 차트 — 서브차트를 켜고 끔 |
| `nodeExporter.tolerations` | 🔴 **아무도 안 읽음 — 조용히 무시** |
| `prometheus-node-exporter.tolerations` | ✅ 서브차트로 전달 |

그래서 서브차트 기본값(`NoSchedule` 하나)이 그대로 남아,
`NoExecute` taint 인 node0 에 node-exporter 가 안 떴습니다.

공식 저장소에도 같은 이슈가 있습니다 (#2081 · #1347).

---

## 고친 파일 2개

| 파일 | |
|---|---|
| `helm-values/kube-prometheus-stack.yaml` | 서브차트 키로 이동 |
| `vagrant/scripts/80-monitoring.sh` | 인라인 values 도 동일하게 |

```yaml
# enabled 는 상위 차트가 읽는다 — 그대로 둔다
nodeExporter:
  enabled: true

# 🔴 실제 설정은 서브차트 이름으로
prometheus-node-exporter:
  tolerations:
    - { operator: Exists, effect: NoSchedule }
    - { operator: Exists, effect: NoExecute }
```

**왜 두 effect 를 다 쓰나**

| 노드 | taint |
|---|---|
| node0 | `CriticalAddonsOnly=true:NoExecute` |
| node4 | `workload=batch:NoSchedule` |
| node5 | `workload=infra:NoSchedule` |

`operator: Exists` 에 `effect` 를 명시하면 **그 effect 만** 견딥니다.
전 노드 지표를 걷으려면 둘 다 필요합니다.

---

## 같은 유형이 더 있는지 전부 점검했습니다

| 차트 | 서브차트 | 우리가 쓴 키 | 판정 |
|---|---|---|---|
| kube-prometheus-stack | `prometheus-node-exporter` | `nodeExporter` | 🔴 **수정함** |
| | `kube-state-metrics` | `kubeStateMetrics` | `enabled` 만 줘서 안전 |
| | `grafana` | `grafana` | 이름 같아 안전 |
| argo-cd | `redis-ha` | `redis-ha` | 이름 같아 안전 |
| jenkins | 없음 | — | 안전 |

**나머지는 문제없습니다.**

> ⚠️ 나중에 `kube-state-metrics` 에 `nodeSelector` 나 `tolerations` 를 주려면
> `kubeStateMetrics:` 가 아니라 **`kube-state-metrics:`** 로 써야 합니다.

---

## 왜 우리는 못 잡았나

`kubectl patch` 로 **실행 중인 DaemonSet 을 직접 고쳐서** 6개를 띄웠습니다.
그래서 **값 파일이 애초에 안 먹고 있다는 걸 몰랐습니다.**

`vagrant destroy` 후 다시 돌리면 또 5개가 됐을 겁니다.

### 앞으로 이런 걸 막으려면

```bash
# 값이 실제로 반영되는지 렌더링으로 확인
helm template kps prometheus-community/kube-prometheus-stack \
  -f helm-values/kube-prometheus-stack.yaml \
  | grep -B2 -A8 "kind: DaemonSet" | grep -A6 tolerations
```

```bash
# 배포 후에도 확인
kubectl get ds kps-prometheus-node-exporter -n monitoring \
  -o jsonpath='{.spec.template.spec.tolerations}' | python3 -m json.tool
```

**`kubectl patch` 는 원인을 가립니다.** 값 파일을 고치고 재배포해서 확인해야 합니다.

---

## 지금 클러스터에 적용

이미 patch 로 6개가 떠 있으면 그대로 두셔도 됩니다.
값 파일 기준으로 다시 맞추려면:

```bash
cd ~/reverdi && git pull
helm upgrade kps prometheus-community/kube-prometheus-stack \
  -n monitoring -f helm-values/kube-prometheus-stack.yaml
```

**확인**

```bash
kubectl get ds kps-prometheus-node-exporter -n monitoring \
  -o jsonpath='{.spec.template.spec.tolerations}' | python3 -m json.tool
kubectl get pod -n monitoring -o wide | grep node-exporter
```

`NoSchedule` · `NoExecute` 둘 다 나오고 **6개**면 정상입니다.

---

## 문서 갱신

`문제해결_기록.md` 에 **19번**으로 추가했습니다.
발견 경위(팀원 지적)와 진단 방법도 함께 적었습니다.
