# 팀원 지적 2건 반영

> 2026-09-04 · 둘 다 맞는 지적이고, 검증 후 반영했습니다.

---

## ① node-exporter 서브차트 키

### 지적
> `nodeExporter` 에서 오류가 나서 `prometheus-node-exporter` 로 바꿨더니 됐다

### 확인 — 맞습니다

`kube-prometheus-stack` 은 node-exporter 를 직접 만들지 않고
**`prometheus-node-exporter` 라는 별도 차트를 가져다 씁니다.**

| 키 | 누가 읽나 |
|---|---|
| `nodeExporter.enabled` | 상위 차트 — 서브차트를 켜고 끔 |
| `nodeExporter.tolerations` | 🔴 **아무도 안 읽음 — 조용히 무시** |
| `prometheus-node-exporter.tolerations` | ✅ 서브차트로 전달 |

공식 저장소에 같은 이슈가 있습니다 (#2081 · #1347).

### 왜 우리는 못 잡았나

`kubectl patch` 로 실행 중인 DaemonSet 을 직접 고쳐 6개를 띄웠습니다.
그래서 **값 파일이 애초에 안 먹고 있다는 걸 몰랐습니다.**
`vagrant destroy` 후 다시 돌리면 또 5개가 됐을 겁니다.

### 고친 것

```yaml
nodeExporter:
  enabled: true          # 상위 차트가 읽는다 — 그대로

prometheus-node-exporter:   # 🔴 서브차트 이름
  tolerations:
    - { operator: Exists, effect: NoSchedule }
    - { operator: Exists, effect: NoExecute }
```

| 파일 | |
|---|---|
| `helm-values/kube-prometheus-stack.yaml` | ✅ |
| `vagrant/scripts/80-monitoring.sh` | ✅ 인라인 values 도 동일하게 |

### 같은 유형을 전부 점검했습니다

| 차트 | 서브차트 | 우리 키 | 판정 |
|---|---|---|---|
| kube-prometheus-stack | `prometheus-node-exporter` | `nodeExporter` | 🔴 **수정** |
| | `kube-state-metrics` | `kubeStateMetrics` | `enabled` 만 줘서 안전 |
| | `grafana` | `grafana` | 이름 같아 안전 |
| argo-cd | `redis-ha` | `redis-ha` | 안전 |
| jenkins | 없음 | — | 안전 |

> ⚠️ 나중에 `kube-state-metrics` 에 노드를 지정하려면
> `kubeStateMetrics:` 가 아니라 **`kube-state-metrics:`** 로 써야 합니다.

---

## ② CRLF 자동 제거

### 제안
> Vagrantfile 에 `fix-line-endings` provisioner 를 넣어
> 복사 직후 `\r` 을 제거하자

### 확인 — 좋은 제안입니다

**순서를 검증했습니다.**

```
file              스크립트 복사
   ↓
fix-line-endings  ← 여기여야 한다
   ↓
common            첫 스크립트 실행
```

뒤로 가면 이미 `00-common.sh` 가 깨진 뒤라 소용이 없습니다.

**이스케이프도 확인했습니다.**

| | |
|---|---|
| Vagrantfile 안 | `'s/\\\\r$//' {} \\\\;` |
| Ruby 해석 후 셸에 | `'s/\r$//' {} \;` |

**실제 동작도 확인했습니다.**

```
전:  a.sh  with CRLF   b.sh  with CRLF   c.txt  with CRLF
후:  a.sh  정상        b.sh  정상        c.txt  with CRLF  ← .sh 만 고침
```

### 부작용 없음

| 호스트 | 결과 |
|---|---|
| Windows | `\r` 제거 |
| macOS · 리눅스 | 애초에 없어 **아무 일도 안 함** |

### `.gitattributes` 와 함께

이미 `*.sh text eol=lf` 를 넣는 방법이 있지만,
**각자 git 설정(`core.autocrlf`)을 타서** 환경에 따라 갈립니다.

**둘 다 두는 게 좋습니다.** 하나는 예방, 하나는 보험입니다.

---

## 문서 갱신

| | |
|---|---|
| `문제해결_기록.md` | 18건 → **19건** (서브차트 = 19번) |
| | 문제 3(CRLF)에 **근본 대책 ②** 추가 |
| | 🔴 **분류 표 합계 불일치 수정** (18 → 19) |
| `팀공유_구축테스트정리.md` | 16건 → 19건 |
| `구축테스트_과정기록.md` | 11건 → 19건 |
| `적용방법.md` | 16건 → 19건 |

### ⚠️ 점검 중 추가로 발견한 것

문서마다 **문제 건수가 제각각**이었습니다.

| 문서 | 표기 | 실제 |
|---|:--:|:--:|
| `문제해결_기록.md` 분류 표 | 18 | **19** |
| `팀공유_구축테스트정리.md` | 16 | **19** |
| `구축테스트_과정기록.md` | 11 | **19** |
| `적용방법.md` | 16 | **19** |

문제를 추가할 때 **본문만 고치고 요약·표를 안 고친 것**이 쌓였습니다.
전부 19로 맞췄고, 분류도 다시 셌습니다.

| 분류 | 건수 |
|---|:--:|
| 환경 · 도구 문제 | 7 |
| **우리 코드 · 설정의 실수** | **8** |
| 이해가 부족했던 것 | 4 |

---

## 적용

이 패키지의 파일을 덮어쓰고 커밋하세요.

```powershell
cd D:\project\Reluxe
# 압축 해제 (7-Zip · 반디집)

git add -A
git status
git commit -m "fix: node-exporter 서브차트 키, Vagrantfile CRLF 자동 제거"
git push
```

### 지금 클러스터에 반영

이미 `kubectl patch` 로 6개가 떠 있으면 그대로 두셔도 됩니다.
값 파일 기준으로 맞추려면:

```bash
cd ~/reverdi && git pull
helm upgrade kps prometheus-community/kube-prometheus-stack \
  -n monitoring -f helm-values/kube-prometheus-stack.yaml
```

**확인**

```bash
kubectl get ds kps-prometheus-node-exporter -n monitoring \
  -o jsonpath='{.spec.template.spec.tolerations}' | python3 -m json.tool
kubectl get pod -n monitoring -o wide | grep node-exporter
```

`NoSchedule` · `NoExecute` 둘 다 나오고 **6개**면 정상입니다.

---

## 팀원에게

> **두 건 다 맞습니다. 그대로 반영했습니다.**
>
> **서브차트** — 저희가 `kubectl patch` 로 당장 되게 만든 탓에
> 값 파일이 안 먹고 있다는 걸 놓쳤습니다. 재구축하면서 잡아주셔서 다행입니다.
> 다른 차트도 같은 문제가 있는지 전부 점검했고, 나머지는 괜찮습니다.
>
> **CRLF** — 순서(`file` → `fix-line-endings` → `common`)와 이스케이프,
> 실제 동작까지 확인했습니다. 다른 OS 에서는 아무 일도 하지 않아 부작용도 없습니다.
