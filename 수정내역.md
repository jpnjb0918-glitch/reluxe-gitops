# 통합 점검 · 수정 내역

`Reluxe.zip`(A+B+C+Jenkinsfile)을 점검하고 고친 목록입니다.

---

## 🔴 고친 것

### 1. registry·minio 파드가 뜨지 못하는 상태였음

문서 ② 설계상 node5 에 `workload=infra:NoSchedule` taint 가 걸립니다.
두 파일 모두 `nodeSelector` 만 있고 **`tolerations` 가 없었습니다.**

`nodeSelector` 는 "어디로 갈지"만 정합니다. toleration 이 없으면
taint 걸린 노드가 파드를 거부해 **Pending 에서 멈춥니다.**

→ `infra/registry.yaml`, `infra/minio.yaml` 에 toleration 추가

### 2. PostgreSQL 버전이 세 곳에서 갈렸음

| | 전 | 후 |
|---|---|---|
| CloudNativePG | 16.4 | **17.2** |
| `pgdump.image` | 17-alpine | 17-alpine |
| Jenkinsfile test | postgres:16 | **postgres:17-alpine** |
| AWS 계획(문서 ①) | RDS PG17 | — |

Vagrant 16 / AWS 17 로 갈리면 마이그레이션이나 `pg_dump` 에서
환경별로 다른 문제가 납니다. **17 로 통일**했습니다.

### 3. 🔴 Jenkinsfile — 실행하면 실패하는 지점 4개

**(a) 한 컨테이너에 모든 도구를 기대**

`agent { label 'buildah' }` 로 buildah 컨테이너 하나만 붙는데,
파이프라인이 `uv` · `helm` · `git` 을 씁니다.
`quay.io/buildah/stable` 에는 그것들이 없어 **command not found** 로 죽습니다.

→ 컨테이너 3개짜리 파드로 변경
  - `python`  — uv · ruff · pytest · alembic
  - `buildah` — 이미지 빌드/푸시
  - `tools`   — helm · git

각 stage 를 `container('이름')` 으로 감쌌습니다.

**(b) test 단계가 `docker run` 으로 DB 를 띄움**

k3s 는 containerd 라 **Docker 데몬이 없습니다.** 이 방식은 실패합니다.
→ Kubernetes 플러그인의 **사이드카 컨테이너**로 Postgres 를 붙였습니다.
  같은 파드라 `127.0.0.1:5432` 로 접근됩니다.

**(c) `IMAGE_TAG` 가 null 이 될 수 있음**

`environment` 블록은 체크아웃 **전에** 평가됩니다.
그 시점에 `env.GIT_COMMIT` 은 아직 채워지지 않아 `.take(7)` 이 실패하거나 빈 값이 됩니다.
→ `준비` stage 의 `script` 블록에서 설정

**(d) `sleep 5` 로 DB 를 기다림**

느린 노드에서는 부족합니다. → 접속 확인 루프(최대 60초)로 변경

**그 외 보완**
- `when { branch 'main' }` — PR 빌드가 배포를 일으키지 않도록
- `git diff --quiet` 로 변경 없을 때 빈 커밋 방지
- `post { always }` 에서 토큰이 든 디렉터리 삭제
- `disableConcurrentBuilds()` · `timeout(60분)`

### 4. 인프라 파일이 폴더 밖에 있었음

`registry.yaml` · `registries.yaml` · `postgres-cluster.yaml` · `minio.yaml`
→ **`infra/`** 로 이동 (문서 ④ 3장 구조와 일치)

### 5. `.gitignore` 문제 둘

- **BOM 이 있어 첫 줄(`helm.exe`)이 무시될 수 있었음** → BOM 제거
- `out-aws.yaml` · `kubeconfig` · `.env` 누락 → 추가

### 6. 렌더링 산출물 삭제

`out.yaml` · `out-aws.yaml` 은 커밋 대상이 아닙니다. 삭제 후 `.gitignore` 에 등록.

---

## ✅ 확인만 하고 그대로 둔 것

| 항목 | 확인 결과 |
|---|---|
| 레지스트리 주소 | `registries.yaml` · `values-vagrant` · Jenkinsfile · Service nodePort **전부 30500** |
| DB 서비스 이름 | Cluster `reluxe-db` → `-rw`/`-ro` 가 A 의 README 안내와 일치 |
| Argo CD 경로 | `charts/reluxe` + `values-vagrant.yaml` 실재 |
| Argo CD 정책 | `prune: true` · `selfHeal: true` · `CreateNamespace=true` · retry 5회 |
| Jenkins 에이전트 taint | C 가 `workload=batch` toleration 을 이미 넣어둠 |
| CNPG `podAntiAffinityType: preferred` | primary/replica 가 다른 노드에 뜸 — 페일오버 시연의 전제 |
| minio 계정 | `envFrom: secretRef` 로 참조. 값이 파일에 없음 |
| `registries.yaml` 에 kind 없음 | 정상. kubectl 대상이 아니라 노드 설정 파일 |

---

## ⚠️ 아직 남은 것 — 팀에서 처리

### 배포 전 반드시

- [ ] **`reluxe-secret` 생성** — 차트는 참조만 합니다
      CloudNativePG 가 만든 `reluxe-db-app` 의 비밀번호를 꺼내
      `DATABASE_URL` / `DATABASE_RO_URL` 에 넣어야 합니다
      ```
      kubectl get secret reluxe-db-app -n reluxe -o jsonpath='{.data.password}' | base64 -d
      ```
- [ ] **`minio-credentials` 생성** (minio.yaml 주석 참조)
- [ ] **노드 라벨** `kubectl label node node1 node2 node3 workload=web`
      `node4 workload=batch` · `node5 workload=infra`
- [ ] **taint** `kubectl taint node node4 workload=batch:NoSchedule`
      `node5 workload=infra:NoSchedule`
- [ ] **`registries.yaml` 을 전 노드(node0~5)에 복사** 후 k3s 재시작
- [ ] Jenkins 자격증명 `gitops-push-token` 등록

### 미구현이라 꺼둔 것

| 항목 | 이유 |
|---|---|
| `aggregate.enabled: false` | `app/aggregate` 모듈 없음 (백엔드 수정요청 6번) |
| `pgdump.enabled: false` | MinIO 업로드 부분이 TODO 로 남아 있음 |
| `secret.yaml` 의 `DATABASE_RO_URL` | 앱이 아직 읽지 않음 (백엔드 수정요청 1번) |

### 확인하지 못한 것

- **`helm lint` · `helm template` 을 이 환경에서 실행하지 못했습니다.**
  (helm 바이너리 다운로드가 네트워크 정책에 막힘)
  문법·논리는 다른 방식으로 검증했으나, **팀 PC 에서 다시 돌려주세요.**
  ```
  helm lint charts/reluxe
  helm template reluxe charts/reluxe -f charts/reluxe/values-vagrant.yaml
  ```
- **Jenkinsfile 은 실제 Jenkins 에서 돌려봐야** 확실합니다.
  특히 `container()` 블록과 사이드카 DB 접속을 확인하세요.
- ~~`alpine/helm` 에 git 포함 여부~~ → **확인 후 해결.**
  Alpine 기반 이미지는 git 을 기본 포함하지 않습니다.
  Jenkinsfile 의 해당 두 단계에 `apk add --no-cache git` 을 넣었습니다.


---

## 추가로 고친 것 (2차 점검)

### 7. `alpine/helm` 에 git 이 없음

Alpine 기반 이미지는 **git 을 기본 포함하지 않습니다.** (확인함)
`chart lint` 와 `update gitops` 단계에서 `git clone` 이 바로 실패합니다.

→ 두 단계 앞에 추가
```sh
command -v git >/dev/null 2>&1 || apk add --no-cache git
```

### 8. `post { always }` 가 원래 오류를 가릴 수 있었음

`container('tools')` 로 감싸면, 앞 단계에서 파드가 못 뜬 경우
**정리 작업 자체가 실패해 진짜 실패 원인이 로그에서 밀려납니다.**

→ 래핑을 제거. `rm` 은 기본 jnlp 컨테이너에도 있습니다.

---

## 최종 검증 결과

| 항목 | 검사 수 | 결과 |
|---|---:|---|
| YAML 문법 (템플릿 제외) | 12 | 통과 |
| Helm 템플릿 구문 균형 | 12 | 통과 |
| toleration ↔ taint | 2 | 통과 |
| PG 버전 정렬 | 3 | 통과 |
| Jenkinsfile ↔ jenkins.yaml 정합 | 9 | 통과 |
| 주소·이름 일치 | 5 | 통과 |
| Argo CD 경로 | 2 | 통과 |
| .gitignore | 7 | 통과 |

> ⚠️ Helm 템플릿은 `{{ }}` 때문에 생 YAML 파싱이 원래 불가능합니다.
> 구문 균형만 확인했고, **실제 렌더링은 `helm template` 으로 확인해야 합니다.**
